exec --no-startup-id bluetoothctl power on
exec --no-startup-id nm-applet
# Start ssh-agent if not already running
# exec --no-startup-id ~/.config/sway/scripts.d/ssh.sh
# exec --no-startup-id export SSH_AUTH_SOCK=%t/ssh-agent.socket


# exec --no-startup-id /usr/bin/gnome-keyring-daemon
# exec --no-startup-id /usr/bin/gnome-keyring-daemon --start --components=pkcs11,secrets,ssh

# exec --no-startup-id export SSH_AUTH_SOCK=/run/user/1000/keyring/ssh
# exec --no-startup-id export SSH_AUTH_SOCK=$XDG_RUNTIME_DIR/keyring/ssh
exec wl-paste --type text --watch cliphist store
exec wl-paste --type image --watch cliphist store

exec env APPIMAGELAUNCHER_DISABLE=TRUE /home/balazshasprai/.joplin/Joplin.AppImage
exec --no-startup-id proton-mail

# for_window [class="Joplin"] {
#     move scratchpad
#     floating enable
# }

for_window [class="Proton Mail"] {
    move scratchpad
    floating enable
}
